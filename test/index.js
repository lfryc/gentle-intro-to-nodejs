'use strict';

var assert = require('assert');
var generated = require('../lib');

describe('generated', function () {
  it('should have unit test!', function () {
    assert(false, 'we expected this package author to add actual unit tests 12.');
  });
});
